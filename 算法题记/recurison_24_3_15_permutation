#include <iostream>
#include<vector>
#include<map>
using namespace std;
int N;
void print(map<int, bool>& nums, int count, vector<int>& s);

int main() {
	cin >> N;
	map<int, bool> nums;
	vector<int> s;
	for (size_t i = 0; i < N; i++) {
		nums[i] = false;
	}
	print(nums, 0, s);
}

void print(map<int, bool>& nums, int count, vector<int>& s) {
	if (count >= N) {
		for (auto& S : s)
			cout << S + 1;
		cout << endl;
		return;
	}
	for (auto& pair : nums) {
		if (!pair.second) {
			s.push_back(pair.first);
			pair.second = 1;
			print(nums, ++count, s);
			count--;
			pair.second = 0;//undo
			s.erase(s.end() - 1);
		}
	}
	return;
}
